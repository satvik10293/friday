# Google Gemini cloud LLM configuration (hosted API — no local weights)
provider: google-generativeai
model: gemini-1.5-flash
api_key_env: GEMINI_API_KEY    # secret lives in .env, never here
temperature: 0.4
max_tokens: 1024
chain_order: 2
