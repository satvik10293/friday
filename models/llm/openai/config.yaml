# OpenAI cloud LLM configuration (hosted API — no local weights)
provider: openai
model: gpt-4o-mini
api_key_env: OPENAI_API_KEY    # secret lives in .env, never here
temperature: 0.4
max_tokens: 1024
chain_order: 3
