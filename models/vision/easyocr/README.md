# EasyOCR (vision/OCR)

Reads on-screen text for visual answers and the proactive watcher
(`core.io.friday_visual`, `core.io.friday_proactive`). CPU mode.

- **Weights:** `.pth` recognition/detection models, downloaded by EasyOCR on first
  use — **gitignored** (not committed). **Milestone:** 3.0.
