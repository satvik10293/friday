# edge-tts (speech/TTS)

FRIDAY's voice (`core.voice.friday_tts`) via Microsoft Edge hosted neural voices.
No weights — a streaming API. Voice/rate/pitch configured in `config.yaml`.
**Milestone:** 3.0.
