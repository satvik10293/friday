"""Friday 3.0 — core package.

Importing this package is intentionally side-effect free, so `from core.X import ...`
never triggers installs, prompts, or banners. Run environment setup explicitly:

    python setup.py
"""

__version__ = "3.0"
